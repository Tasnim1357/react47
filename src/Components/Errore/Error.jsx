import React from 'react';
import PropTypes from 'prop-types';

const Error = props => {
    return (
        <div>
            <h2>OOPs error!!</h2>
        </div>
    );
};

Error.propTypes = {
    
};

export default Error;