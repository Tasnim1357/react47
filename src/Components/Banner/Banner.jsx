import React from 'react';
import PropTypes from 'prop-types';

const Banner = props => {
    return (
        <div>
            <h2 className='text-center text-5xl'>Home Banner Page</h2>
        </div>
    );
};

Banner.propTypes = {
    
};

export default Banner;